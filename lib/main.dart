import 'package:flutter/material.dart';

main() {
  // 1. runApp函数
  runApp(
    Center(
      child: ,
    )
      Text(
    "Hello World",
    textDirection: TextDirection.ltr,
    style: TextStyle(
      fontSize: 30,
      color: Colors.orange
    ),
  ));
}
